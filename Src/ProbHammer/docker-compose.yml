services:
  web:
    image: ghcr.io/pmcevoy/probhammer:${BUILD_TAG:-latest}
    build:
      context: .
      dockerfile: src/ProbHammer.Web/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
